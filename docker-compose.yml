version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: vakitniyet-postgres
    environment:
      POSTGRES_DB: vakitniyet
      POSTGRES_USER: vakitniyet
      POSTGRES_PASSWORD: vakitniyet
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vakitniyet"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: vakitniyet-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
